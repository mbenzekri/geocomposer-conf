{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://geocomposer.local/config-and-rules.schema.json",
  "title": "GeoComposer config with expansion rules",
  "description": "Schema éditeur autorisant la configuration GeoComposer avant expansion des $defs, $ref locaux et variables d’environnement",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "services",
    "sources",
    "layers"
  ],
  "properties": {
    "$schema": {
      "anyOf": [
        {
          "type": "string",
          "description": "JSON Schema URI used by editors for validation and autocompletion. Usually keep this value unchanged. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "JSON Schema URI used by editors for validation and autocompletion. Usually keep this value unchanged. (string or \"$s{ENV_VAR|default}\")"
    },
    "server": {
      "anyOf": [
        {
          "$ref": "#/$defs/server",
          "description": "Optional HTTP server and logging configuration. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "port": {
              "anyOf": [
                {
                  "description": "HTTP server port. Minimum: 0. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")",
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 65535
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "HTTP server port. Minimum: 0. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
            },
            "logLevel": {
              "anyOf": [
                {
                  "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`. (\"DEBUG\", \"LOG\", \"WARN\", \"ERROR\", \"NONE\" or \"$s{ENV_VAR|default}\")",
                  "type": "string",
                  "enum": [
                    "DEBUG",
                    "LOG",
                    "WARN",
                    "ERROR",
                    "NONE"
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`. (\"DEBUG\", \"LOG\", \"WARN\", \"ERROR\", \"NONE\" or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Optional HTTP server and logging configuration. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Optional HTTP server and logging configuration. (object or \"#/$defs/nom\")"
    },
    "services": {
      "anyOf": [
        {
          "$ref": "#/$defs/services",
          "description": "Enabled service endpoints. Required. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "wms": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmsService",
                  "description": "WMS service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "maxWidth": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxHeight": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "WMS service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "WMS service configuration. (object or \"#/$defs/nom\")"
            },
            "api": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ogcFeaturesService",
                  "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "defaultLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
            },
            "xyz": {
              "anyOf": [
                {
                  "$ref": "#/$defs/xyzService",
                  "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "maxScaleFactor": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
            },
            "wmts": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmtsService",
                  "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Enabled service endpoints. Required. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Enabled service endpoints. Required. (object or \"#/$defs/nom\")"
    },
    "tilesets": {
      "anyOf": [
        {
          "$ref": "#/$defs/tilesets",
          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (object or \"#/$defs/nom\")"
    },
    "tileMatrixSets": {
      "anyOf": [
        {
          "$ref": "#/$defs/tileMatrixSets",
          "description": "Optional tile matrix set registry. Built-in `WebMercatorQuad` is available when omitted. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Optional tile matrix set registry. Built-in `WebMercatorQuad` is available when omitted. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Optional tile matrix set registry. Built-in `WebMercatorQuad` is available when omitted. (object or \"#/$defs/nom\")"
    },
    "crs": {
      "anyOf": [
        {
          "$ref": "#/$defs/crsRegistry",
          "description": "Optional registry of custom or named crs. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Optional registry of custom or named crs. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Optional registry of custom or named crs. (object or \"#/$defs/nom\")"
    },
    "sources": {
      "anyOf": [
        {
          "$ref": "#/$defs/sources",
          "description": "Data source registry. Required. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Data source registry. Required. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Data source registry. Required. (object or \"#/$defs/nom\")"
    },
    "styles": {
      "anyOf": [
        {
          "$ref": "#/$defs/styles",
          "description": "Optional style registry referenced by layers and tilesets. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Optional style registry referenced by layers and tilesets. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Optional style registry referenced by layers and tilesets. (object or \"#/$defs/nom\")"
    },
    "layers": {
      "anyOf": [
        {
          "$ref": "#/$defs/layers",
          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
    },
    "$defs": {
      "type": "object",
      "additionalProperties": {
        "description": "Valeur JSON libre utilisable dans la section racine $defs de la configuration"
      },
      "description": "Définitions de configuration réutilisables avant expansion"
    }
  },
  "$defs": {
    "httpPath": {
      "anyOf": [
        {
          "title": "HTTP path",
          "type": "string",
          "pattern": "^/",
          "description": "HTTP route path used by a service endpoint. Pattern `^/`: the path must start with `/`, for example `/wms`, `/api`, `/tiles`. Pattern `^/`: the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "HTTP route path used by a service endpoint. Pattern `^/`: the path must start with `/`, for example `/wms`, `/api`, `/tiles`. Pattern `^/`: the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
    },
    "describedEntry": {
      "anyOf": [
        {
          "title": "Common descriptive fields",
          "type": "object",
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            }
          },
          "description": "Reusable descriptive metadata shared by sources, layers, styles and tilesets. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Reusable descriptive metadata shared by sources, layers, styles and tilesets. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Reusable descriptive metadata shared by sources, layers, styles and tilesets. (object or \"#/$defs/nom\")"
    },
    "serviceInfo": {
      "anyOf": [
        {
          "title": "Common service fields",
          "type": "object",
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
            }
          },
          "description": "Reusable service metadata for OGC-like services. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Reusable service metadata for OGC-like services. (object or \"#/$defs/nom\")"
        }
      ],
      "description": "Reusable service metadata for OGC-like services. (object or \"#/$defs/nom\")"
    },
    "bbox": {
      "anyOf": [
        {
          "title": "Bounding box",
          "type": "array",
          "prefixItems": [
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Minimum X coordinate of the bounding box. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Minimum X coordinate of the bounding box. (number or \"$f{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Minimum Y coordinate of the bounding box. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Minimum Y coordinate of the bounding box. (number or \"$f{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Maximum X coordinate of the bounding box. Must be greater than minimum X at runtime. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum X coordinate of the bounding box. Must be greater than minimum X at runtime. (number or \"$f{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Maximum Y coordinate of the bounding box. Must be greater than minimum Y at runtime. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum Y coordinate of the bounding box. Must be greater than minimum Y at runtime. (number or \"$f{ENV_VAR|default}\")"
            }
          ],
          "items": false,
          "minItems": 4,
          "maxItems": 4,
          "description": "Bounding box as `[minX, minY, maxX, maxY]` in the CRS of the related layer or request. Exactly 4 numbers are required. Array length: minimum 4, maximum 4. (array or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        }
      ],
      "description": "Bounding box as `[minX, minY, maxX, maxY]` in the CRS of the related layer or request. Exactly 4 numbers are required. Array length: minimum 4, maximum 4. (array or \"#/$defs/nom\")"
    },
    "server": {
      "anyOf": [
        {
          "description": "Server runtime configuration. (object or \"#/$defs/nom\")",
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "port": {
              "anyOf": [
                {
                  "description": "HTTP server port. Minimum: 0. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")",
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 65535
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "HTTP server port. Minimum: 0. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
            },
            "logLevel": {
              "anyOf": [
                {
                  "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`. (\"DEBUG\", \"LOG\", \"WARN\", \"ERROR\", \"NONE\" or \"$s{ENV_VAR|default}\")",
                  "type": "string",
                  "enum": [
                    "DEBUG",
                    "LOG",
                    "WARN",
                    "ERROR",
                    "NONE"
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`. (\"DEBUG\", \"LOG\", \"WARN\", \"ERROR\", \"NONE\" or \"$s{ENV_VAR|default}\")"
            }
          }
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "port": {
              "anyOf": [
                {
                  "description": "HTTP server port. Minimum: 0. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")",
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 65535
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "HTTP server port. Minimum: 0. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
            },
            "logLevel": {
              "anyOf": [
                {
                  "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`. (\"DEBUG\", \"LOG\", \"WARN\", \"ERROR\", \"NONE\" or \"$s{ENV_VAR|default}\")",
                  "type": "string",
                  "enum": [
                    "DEBUG",
                    "LOG",
                    "WARN",
                    "ERROR",
                    "NONE"
                  ]
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`. (\"DEBUG\", \"LOG\", \"WARN\", \"ERROR\", \"NONE\" or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Server runtime configuration. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Server runtime configuration. (object or \"#/$defs/nom\")"
    },
    "services": {
      "anyOf": [
        {
          "title": "Enabled services",
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "properties": {
            "wms": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmsService",
                  "description": "WMS service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "maxWidth": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxHeight": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "WMS service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "WMS service configuration. (object or \"#/$defs/nom\")"
            },
            "api": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ogcFeaturesService",
                  "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "defaultLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
            },
            "xyz": {
              "anyOf": [
                {
                  "$ref": "#/$defs/xyzService",
                  "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "maxScaleFactor": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
            },
            "wmts": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmtsService",
                  "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
            }
          },
          "description": "Enabled GeoComposer services. At least one of `wms`, `api`, `xyz`, or `wmts` must be present. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "wms": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmsService",
                  "description": "WMS service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "maxWidth": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxHeight": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "WMS service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "WMS service configuration. (object or \"#/$defs/nom\")"
            },
            "api": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ogcFeaturesService",
                  "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "defaultLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "OGC API Features service configuration. (object or \"#/$defs/nom\")"
            },
            "xyz": {
              "anyOf": [
                {
                  "$ref": "#/$defs/xyzService",
                  "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "maxScaleFactor": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
            },
            "wmts": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmtsService",
                  "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "WMTS service configuration. (object or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Enabled GeoComposer services. At least one of `wms`, `api`, `xyz`, or `wmts` must be present. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Enabled GeoComposer services. At least one of `wms`, `api`, `xyz`, or `wmts` must be present. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
    },
    "wmsService": {
      "anyOf": [
        {
          "title": "WMS service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "title"
          ],
          "properties": {
            "maxWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "WMS service configuration. Requires `title`. Use `supportedCrs` to restrict advertised CRS values and `layers` to expose only selected layers. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
            },
            "maxWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum WMS map image width in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum WMS map image height in pixels. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "WMS service configuration. Requires `title`. Use `supportedCrs` to restrict advertised CRS values and `layers` to expose only selected layers. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "WMS service configuration. Requires `title`. Use `supportedCrs` to restrict advertised CRS values and `layers` to expose only selected layers. (object or \"#/$defs/nom\")"
    },
    "ogcFeaturesService": {
      "anyOf": [
        {
          "title": "OGC API Features service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "title"
          ],
          "properties": {
            "defaultLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "OGC API Features service configuration. Requires `title`. Use `defaultLimit` and `maxLimit` to control pagination. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
            },
            "defaultLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "OGC API Features service configuration. Requires `title`. Use `defaultLimit` and `maxLimit` to control pagination. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "OGC API Features service configuration. Requires `title`. Use `defaultLimit` and `maxLimit` to control pagination. (object or \"#/$defs/nom\")"
    },
    "xyzService": {
      "anyOf": [
        {
          "title": "XYZ service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
            }
          ],
          "properties": {
            "maxScaleFactor": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
            },
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
            },
            "maxScaleFactor": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
            },
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "XYZ tile service configuration. (object or \"#/$defs/nom\")"
    },
    "wmtsService": {
      "anyOf": [
        {
          "title": "WMTS service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "title"
          ],
          "properties": {
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "WMTS service configuration. Requires `title`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata. (string or \"$s{ENV_VAR|default}\")"
            },
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses. (string or \"$s{ENV_VAR|default}\")"
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "WMTS service configuration. Requires `title`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "WMTS service configuration. Requires `title`. (object or \"#/$defs/nom\")"
    },
    "tilesetRefs": {
      "anyOf": [
        {
          "title": "Tileset name list",
          "type": "array",
          "minItems": 1,
          "items": {
            "anyOf": [
              {
                "type": "string",
                "description": "Tileset name. Must match a key from the top-level `tilesets` registry. (string or \"$s{ENV_VAR|default}\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "string",
                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
              },
              {
                "type": "string",
                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
              }
            ],
            "description": "Tileset name. Must match a key from the top-level `tilesets` registry. (string or \"$s{ENV_VAR|default}\")"
          },
          "uniqueItems": true,
          "description": "List of tileset names. Each value must match a key from the top-level `tilesets` registry. At least one unique value is required. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        }
      ],
      "description": "List of tileset names. Each value must match a key from the top-level `tilesets` registry. At least one unique value is required. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
    },
    "tilesets": {
      "anyOf": [
        {
          "title": "Tileset registry",
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/tileset",
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "tileMatrixSet": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "formats": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "uniqueItems": true,
                        "items": {
                          "anyOf": [
                            {
                              "enum": [
                                "image/png",
                                "application/geo+json",
                                "application/vnd.mapbox-vector-tile"
                              ],
                              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "string",
                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                            },
                            {
                              "type": "string",
                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                            }
                          ],
                          "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                        },
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
                  },
                  "minZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      }
                    ],
                    "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                  },
                  "maxZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      }
                    ],
                    "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
                  },
                  "cacheControl": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "vector": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/vectorTileOptions",
                        "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "extent": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "buffer": {
                            "anyOf": [
                              {
                                "type": "number",
                                "minimum": 0,
                                "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                          },
                          "generalization": {
                            "anyOf": [
                              {
                                "type": "object",
                                "additionalProperties": false,
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                  }
                                },
                                "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                  }
                                },
                                "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")",
                                "additionalProperties": false
                              }
                            ],
                            "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                          },
                          "geojsonPrecision": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 0,
                                "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "maxFeatures": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                          }
                        },
                        "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                      }
                    ],
                    "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                  },
                  "layers": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "anyOf": [
                            {
                              "anyOf": [
                                {
                                  "anyOf": [
                                    {
                                      "type": "string",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                                },
                                {
                                  "anyOf": [
                                    {
                                      "$ref": "#/$defs/tilesetLayer",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "object",
                                      "required": [
                                        "$ref"
                                      ],
                                      "properties": {
                                        "layer": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                            }
                                          ],
                                          "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                        },
                                        "style": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                            }
                                          ],
                                          "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                        },
                                        "$ref": {
                                          "type": "string",
                                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                        }
                                      },
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                                }
                              ],
                              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "string",
                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                            },
                            {
                              "type": "string",
                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                            }
                          ],
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                        },
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
          },
          "description": "Named tileset registry. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Named tileset registry. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/tileset",
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "tileMatrixSet": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "formats": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "uniqueItems": true,
                        "items": {
                          "anyOf": [
                            {
                              "enum": [
                                "image/png",
                                "application/geo+json",
                                "application/vnd.mapbox-vector-tile"
                              ],
                              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "string",
                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                            },
                            {
                              "type": "string",
                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                            }
                          ],
                          "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                        },
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
                  },
                  "minZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      }
                    ],
                    "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                  },
                  "maxZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      }
                    ],
                    "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
                  },
                  "cacheControl": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "vector": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/vectorTileOptions",
                        "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "extent": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "buffer": {
                            "anyOf": [
                              {
                                "type": "number",
                                "minimum": 0,
                                "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                          },
                          "generalization": {
                            "anyOf": [
                              {
                                "type": "object",
                                "additionalProperties": false,
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                  }
                                },
                                "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                  }
                                },
                                "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")",
                                "additionalProperties": false
                              }
                            ],
                            "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                          },
                          "geojsonPrecision": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 0,
                                "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "maxFeatures": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                          }
                        },
                        "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                      }
                    ],
                    "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                  },
                  "layers": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "anyOf": [
                            {
                              "anyOf": [
                                {
                                  "anyOf": [
                                    {
                                      "type": "string",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                                },
                                {
                                  "anyOf": [
                                    {
                                      "$ref": "#/$defs/tilesetLayer",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "object",
                                      "required": [
                                        "$ref"
                                      ],
                                      "properties": {
                                        "layer": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                            }
                                          ],
                                          "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                        },
                                        "style": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                            }
                                          ],
                                          "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                        },
                                        "$ref": {
                                          "type": "string",
                                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                        }
                                      },
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                                }
                              ],
                              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "string",
                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                            },
                            {
                              "type": "string",
                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                            }
                          ],
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                        },
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
          }
        }
      ],
      "description": "Named tileset registry. (object or \"#/$defs/nom\")"
    },
    "tileMatrixSets": {
      "anyOf": [
        {
          "title": "Tile matrix set registry",
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/tileMatrixSet",
                "description": "Value for each named tile matrix set. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "crs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "supportedCrs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "tileMatrices": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "anyOf": [
                            {
                              "$ref": "#/$defs/tileMatrix",
                              "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "object",
                              "required": [
                                "$ref"
                              ],
                              "properties": {
                                "id": {
                                  "anyOf": [
                                    {
                                      "type": "string",
                                      "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "scaleDenominator": {
                                  "anyOf": [
                                    {
                                      "type": "number",
                                      "exclusiveMinimum": 0,
                                      "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                                },
                                "cellSize": {
                                  "anyOf": [
                                    {
                                      "type": "number",
                                      "exclusiveMinimum": 0,
                                      "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                                },
                                "topLeftCorner": {
                                  "anyOf": [
                                    {
                                      "type": "array",
                                      "minItems": 2,
                                      "maxItems": 2,
                                      "prefixItems": [
                                        {
                                          "anyOf": [
                                            {
                                              "type": "number",
                                              "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            }
                                          ],
                                          "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                        },
                                        {
                                          "anyOf": [
                                            {
                                              "type": "number",
                                              "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            }
                                          ],
                                          "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                        }
                                      ],
                                      "items": false,
                                      "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    }
                                  ],
                                  "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                                },
                                "tileWidth": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "tileHeight": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "matrixWidth": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "matrixHeight": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "$ref": {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                }
                              },
                              "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                            }
                          ],
                          "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                        },
                        "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named tile matrix set. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named tile matrix set. (object or \"#/$defs/nom\")"
          },
          "description": "Named tile matrix set registry. Built-in `WebMercatorQuad` is always available. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Named tile matrix set registry. Built-in `WebMercatorQuad` is always available. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/tileMatrixSet",
                "description": "Value for each named tile matrix set. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "crs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "supportedCrs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "tileMatrices": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "anyOf": [
                            {
                              "$ref": "#/$defs/tileMatrix",
                              "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "object",
                              "required": [
                                "$ref"
                              ],
                              "properties": {
                                "id": {
                                  "anyOf": [
                                    {
                                      "type": "string",
                                      "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "scaleDenominator": {
                                  "anyOf": [
                                    {
                                      "type": "number",
                                      "exclusiveMinimum": 0,
                                      "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                                },
                                "cellSize": {
                                  "anyOf": [
                                    {
                                      "type": "number",
                                      "exclusiveMinimum": 0,
                                      "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                                },
                                "topLeftCorner": {
                                  "anyOf": [
                                    {
                                      "type": "array",
                                      "minItems": 2,
                                      "maxItems": 2,
                                      "prefixItems": [
                                        {
                                          "anyOf": [
                                            {
                                              "type": "number",
                                              "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            }
                                          ],
                                          "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                        },
                                        {
                                          "anyOf": [
                                            {
                                              "type": "number",
                                              "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                            }
                                          ],
                                          "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                        }
                                      ],
                                      "items": false,
                                      "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    }
                                  ],
                                  "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                                },
                                "tileWidth": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "tileHeight": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "matrixWidth": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "matrixHeight": {
                                  "anyOf": [
                                    {
                                      "type": "integer",
                                      "minimum": 1,
                                      "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    }
                                  ],
                                  "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                                },
                                "$ref": {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                }
                              },
                              "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                            }
                          ],
                          "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                        },
                        "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named tile matrix set. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named tile matrix set. (object or \"#/$defs/nom\")"
          }
        }
      ],
      "description": "Named tile matrix set registry. Built-in `WebMercatorQuad` is always available. (object or \"#/$defs/nom\")"
    },
    "tileMatrixSet": {
      "anyOf": [
        {
          "title": "Tile matrix set",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "crs",
            "tileMatrices"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
            },
            "crs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
            },
            "tileMatrices": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/tileMatrix",
                        "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "id": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "scaleDenominator": {
                            "anyOf": [
                              {
                                "type": "number",
                                "exclusiveMinimum": 0,
                                "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                          },
                          "cellSize": {
                            "anyOf": [
                              {
                                "type": "number",
                                "exclusiveMinimum": 0,
                                "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                          },
                          "topLeftCorner": {
                            "anyOf": [
                              {
                                "type": "array",
                                "minItems": 2,
                                "maxItems": 2,
                                "prefixItems": [
                                  {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                  },
                                  {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                  }
                                ],
                                "items": false,
                                "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              }
                            ],
                            "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                          },
                          "tileWidth": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "tileHeight": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "matrixWidth": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "matrixHeight": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                          }
                        },
                        "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                      }
                    ],
                    "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                  },
                  "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
            }
          },
          "description": "Tile matrix set definition with explicit matrices. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in WMTS capabilities. (string or \"$s{ENV_VAR|default}\")"
            },
            "crs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate reference system code used by this tile matrix set. (string or \"$s{ENV_VAR|default}\")"
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "WMTS SupportedCRS URI. Defaults to an OGC EPSG URN for EPSG codes. (string or \"$s{ENV_VAR|default}\")"
            },
            "tileMatrices": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/tileMatrix",
                        "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "id": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "scaleDenominator": {
                            "anyOf": [
                              {
                                "type": "number",
                                "exclusiveMinimum": 0,
                                "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                          },
                          "cellSize": {
                            "anyOf": [
                              {
                                "type": "number",
                                "exclusiveMinimum": 0,
                                "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                          },
                          "topLeftCorner": {
                            "anyOf": [
                              {
                                "type": "array",
                                "minItems": 2,
                                "maxItems": 2,
                                "prefixItems": [
                                  {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                                  },
                                  {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      }
                                    ],
                                    "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                                  }
                                ],
                                "items": false,
                                "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              }
                            ],
                            "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                          },
                          "tileWidth": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "tileHeight": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "matrixWidth": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "matrixHeight": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              }
                            ],
                            "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                          }
                        },
                        "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                      }
                    ],
                    "description": "Tile matrix definition. (object or \"#/$defs/nom\")"
                  },
                  "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Ordered tile matrix definitions. The array index is the internal zoom used by XYZ and tileset minZoom/maxZoom. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Tile matrix set definition with explicit matrices. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Tile matrix set definition with explicit matrices. (object or \"#/$defs/nom\")"
    },
    "tileMatrix": {
      "anyOf": [
        {
          "title": "Tile matrix",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "scaleDenominator",
            "topLeftCorner",
            "tileWidth",
            "tileHeight",
            "matrixWidth",
            "matrixHeight"
          ],
          "properties": {
            "id": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
            },
            "scaleDenominator": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
            },
            "cellSize": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
            },
            "topLeftCorner": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 2,
                  "maxItems": 2,
                  "prefixItems": [
                    {
                      "anyOf": [
                        {
                          "type": "number",
                          "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                    },
                    {
                      "anyOf": [
                        {
                          "type": "number",
                          "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                    }
                  ],
                  "items": false,
                  "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
            },
            "tileWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
            },
            "tileHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
            },
            "matrixWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
            },
            "matrixHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
            }
          },
          "description": "Explicit WMTS tile matrix definition. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "id": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "WMTS TileMatrix identifier. Defaults to the matrix array index. (string or \"$s{ENV_VAR|default}\")"
            },
            "scaleDenominator": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "WMTS scale denominator for this matrix. (number or \"$f{ENV_VAR|default}\")"
            },
            "cellSize": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Cell size in tile matrix CRS units per pixel. Defaults to `scaleDenominator * 0.00028`, which is correct for metre-based CRS. (number or \"$f{ENV_VAR|default}\")"
            },
            "topLeftCorner": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 2,
                  "maxItems": 2,
                  "prefixItems": [
                    {
                      "anyOf": [
                        {
                          "type": "number",
                          "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Top-left X coordinate. (number or \"$f{ENV_VAR|default}\")"
                    },
                    {
                      "anyOf": [
                        {
                          "type": "number",
                          "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Top-left Y coordinate. (number or \"$f{ENV_VAR|default}\")"
                    }
                  ],
                  "items": false,
                  "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Top-left corner of the tile matrix in the tile matrix set CRS. (array or \"#/$defs/nom\")"
            },
            "tileWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Tile width in pixels. (integer or \"$i{ENV_VAR|default}\")"
            },
            "tileHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Tile height in pixels. (integer or \"$i{ENV_VAR|default}\")"
            },
            "matrixWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of tile columns in this matrix. (integer or \"$i{ENV_VAR|default}\")"
            },
            "matrixHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of tile rows in this matrix. (integer or \"$i{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Explicit WMTS tile matrix definition. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Explicit WMTS tile matrix definition. (object or \"#/$defs/nom\")"
    },
    "tileset": {
      "anyOf": [
        {
          "title": "Tileset",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "formats",
            "layers"
          ],
          "properties": {
            "tileMatrixSet": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
            },
            "formats": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "uniqueItems": true,
                  "items": {
                    "anyOf": [
                      {
                        "enum": [
                          "image/png",
                          "application/geo+json",
                          "application/vnd.mapbox-vector-tile"
                        ],
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "minZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
            },
            "cacheControl": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
            },
            "vector": {
              "anyOf": [
                {
                  "$ref": "#/$defs/vectorTileOptions",
                  "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "extent": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "buffer": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "generalization": {
                      "anyOf": [
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                            }
                          },
                          "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                            },
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")",
                          "additionalProperties": false
                        }
                      ],
                      "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                    },
                    "geojsonPrecision": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxFeatures": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "anyOf": [
                      {
                        "anyOf": [
                          {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          {
                            "anyOf": [
                              {
                                "$ref": "#/$defs/tilesetLayer",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "layer": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                      }
                                    ],
                                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                  },
                                  "style": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                      }
                                    ],
                                    "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                  }
                                },
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                          }
                        ],
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "Tileset definition. Requires `formats` and `layers`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "tileMatrixSet": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Tile matrix set identifier. Must reference a built-in or top-level `tileMatrixSets` entry. (string or \"$s{ENV_VAR|default}\")"
            },
            "formats": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "uniqueItems": true,
                  "items": {
                    "anyOf": [
                      {
                        "enum": [
                          "image/png",
                          "application/geo+json",
                          "application/vnd.mapbox-vector-tile"
                        ],
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. (\"image/png\", \"application/geo+json\", \"application/vnd.mapbox-vector-tile\" or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique. (array or \"#/$defs/nom\")"
            },
            "minZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Minimum zoom level available for the tileset. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`. (integer or \"$i{ENV_VAR|default}\")"
            },
            "cacheControl": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`. (string or \"$s{ENV_VAR|default}\")"
            },
            "vector": {
              "anyOf": [
                {
                  "$ref": "#/$defs/vectorTileOptions",
                  "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "extent": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "buffer": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "generalization": {
                      "anyOf": [
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                            }
                          },
                          "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                            },
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")",
                          "additionalProperties": false
                        }
                      ],
                      "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                    },
                    "geojsonPrecision": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "maxFeatures": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Vector tile generation options. (object or \"#/$defs/nom\")"
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "anyOf": [
                      {
                        "anyOf": [
                          {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                          },
                          {
                            "anyOf": [
                              {
                                "$ref": "#/$defs/tilesetLayer",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "layer": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                      }
                                    ],
                                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                                  },
                                  "style": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                      }
                                    ],
                                    "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                  }
                                },
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (object or \"#/$defs/nom\")"
                          }
                        ],
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Tileset definition. Requires `formats` and `layers`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "Tileset definition. Requires `formats` and `layers`. (object or \"#/$defs/nom\")"
    },
    "vectorTileOptions": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "extent": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
            },
            "buffer": {
              "anyOf": [
                {
                  "type": "number",
                  "minimum": 0,
                  "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
            },
            "generalization": {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                    }
                  },
                  "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
            },
            "geojsonPrecision": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxFeatures": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            }
          },
          "description": "Options used when generating vector tiles or GeoJSON-like tile output. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "extent": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096. (integer or \"$i{ENV_VAR|default}\")"
            },
            "buffer": {
              "anyOf": [
                {
                  "type": "number",
                  "minimum": 0,
                  "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary. (number or \"$f{ENV_VAR|default}\")"
            },
            "generalization": {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                    }
                  },
                  "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Geometry simplification options applied during vector tile generation. (object or \"#/$defs/nom\")"
            },
            "geojsonPrecision": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
            },
            "maxFeatures": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Maximum number of features emitted per tile. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Options used when generating vector tiles or GeoJSON-like tile output. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Options used when generating vector tiles or GeoJSON-like tile output. (object or \"#/$defs/nom\")"
    },
    "tilesetLayer": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "layer"
          ],
          "properties": {
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
            }
          },
          "description": "Layer entry inside a tileset. Allows selecting a layer and optionally overriding its style. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Layer entry inside a tileset. Allows selecting a layer and optionally overriding its style. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Layer entry inside a tileset. Allows selecting a layer and optionally overriding its style. (object or \"#/$defs/nom\")"
    },
    "crsRegistry": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/crs",
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "name": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "proj4": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
          },
          "description": "Named collection of crs. ex: 'EPSG:4326' : { 'title': 'WGS84' }  (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Named collection of crs. ex: 'EPSG:4326' : { 'title': 'WGS84' }  (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/crs",
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "name": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "proj4": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
          }
        }
      ],
      "description": "Named collection of crs. ex: 'EPSG:4326' : { 'title': 'WGS84' }  (object or \"#/$defs/nom\")"
    },
    "crs": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "title"
          ],
          "properties": {
            "name": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "proj4": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
            }
          },
          "description": "crs metadata. Requires a human-readable `title`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "name": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Machine or external name. For crs, this may be the crs name known by the crs library. (string or \"$s{ENV_VAR|default}\")"
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "proj4": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Proj4 definition used to register a CRS that is not known by the projection library, for example definitions from epsg.io. (string or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "crs metadata. Requires a human-readable `title`. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "crs metadata. Requires a human-readable `title`. (object or \"#/$defs/nom\")"
    },
    "sources": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/source",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Source registry. At least one source is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Source registry. At least one source is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/source",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Source registry. At least one source is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
    },
    "source": {
      "anyOf": [
        {
          "oneOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/csvSource",
                  "description": "CSV source variant. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "csv",
                          "description": "Source discriminator. Must be `csv`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `csv`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "encoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "delimiter": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "maxLength": 1,
                          "description": "Single-character CSV field delimiter. Defaults to `,`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Single-character CSV field delimiter. Defaults to `,`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "CSV column containing WKT geometry. Defaults to `geometry`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "CSV column containing WKT geometry. Defaults to `geometry`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "CSV column used as feature id. Defaults to `id`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "CSV column used as feature id. Defaults to `id`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "CSV source variant. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "CSV source variant. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/geojsonSource",
                  "description": "First allowed object shape for this union. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "geojson",
                          "description": "Source discriminator. Must be `geojson`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `geojson`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "encoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "First allowed object shape for this union. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "First allowed object shape for this union. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/gmlSource",
                  "description": "Second allowed object shape for this union. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "gml",
                          "description": "Source discriminator. Must be `gml`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `gml`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "encoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "featureElementNames": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                    },
                    "geometryPropertyNames": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                    },
                    "axisOrder": {
                      "anyOf": [
                        {
                          "enum": [
                            "auto",
                            "xy",
                            "yx"
                          ],
                          "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`. (\"auto\", \"xy\", \"yx\" or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`. (\"auto\", \"xy\", \"yx\" or \"$s{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Second allowed object shape for this union. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Second allowed object shape for this union. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/shpSource",
                  "description": "Shapefile source variant. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "shp",
                          "description": "Source discriminator. Must be `shp`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `shp`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "shpPath": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Path to the `.shp` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Path to the `.shp` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "dbfPath": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Path to the `.dbf` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Path to the `.dbf` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "dbfEncoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Shapefile source variant. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Shapefile source variant. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/gpkgSource",
                  "description": "GeoPackage source variant. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "gpkg",
                          "description": "Source discriminator. Must be `gpkg`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `gpkg`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/gpkgDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "GeoPackage source variant. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "GeoPackage source variant. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/postgisSource",
                  "description": "PostGIS source variant. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "postgis",
                          "description": "Source discriminator. Must be `postgis`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `postgis`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "connection": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/postgisConnection",
                          "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sqlDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                    },
                    "batchSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "extentStrategy": {
                      "anyOf": [
                        {
                          "enum": [
                            "estimated",
                            "exact",
                            "none"
                          ],
                          "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`. (\"estimated\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`. (\"estimated\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "PostGIS source variant. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "PostGIS source variant. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/mssqlSource",
                  "description": "SQL Server source variant. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "mssql",
                          "description": "Source discriminator. Must be `mssql`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `mssql`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "connection": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/mssqlConnection",
                          "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sqlDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                    },
                    "batchSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "extentStrategy": {
                      "anyOf": [
                        {
                          "enum": [
                            "exact",
                            "none"
                          ],
                          "description": "Strategy used to compute source extents. SQL Server allowed values: `exact`, `none`. Allowed values: `exact`, `none`. (\"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Strategy used to compute source extents. SQL Server allowed values: `exact`, `none`. Allowed values: `exact`, `none`. (\"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "SQL Server source variant. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "SQL Server source variant. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleSource",
                  "description": "Oracle source variant. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "oracle",
                          "description": "Source discriminator. Must be `oracle`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Source discriminator. Must be `oracle`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "connection": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/oracleConnection",
                          "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sqlDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                    },
                    "batchSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "extentStrategy": {
                      "anyOf": [
                        {
                          "enum": [
                            "metadata",
                            "exact",
                            "none"
                          ],
                          "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`. (\"metadata\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`. (\"metadata\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                    },
                    "indexes": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sourceIndexes"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ]
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Oracle source variant. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Oracle source variant. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "Data source definition. Choose exactly one supported source type: CSV, GeoJSON, GML, Shapefile, GeoPackage, PostGIS, SQL Server or Oracle."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        }
      ],
      "description": "Data source definition. Choose exactly one supported source type: CSV, GeoJSON, GML, Shapefile, GeoPackage, PostGIS, SQL Server or Oracle."
    },
    "sourceIndexes": {
      "anyOf": [
        {
          "description": "Source index configuration. Use `true` to build the minimal file indexes (`record` and `rtree`). Object form is reserved for explicit index families such as property indexes. (boolean or \"$b{ENV_VAR|default}\")",
          "oneOf": [
            {
              "anyOf": [
                {
                  "const": true,
                  "description": "Build the minimal indexes for this source. (boolean or \"$b{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Build the minimal indexes for this source. (boolean or \"$b{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": true,
                  "description": "Future explicit index configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Future explicit index configuration. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Future explicit index configuration. (object or \"#/$defs/nom\")"
            }
          ]
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        }
      ],
      "description": "Source index configuration. Use `true` to build the minimal file indexes (`record` and `rtree`). Object form is reserved for explicit index families such as property indexes. (boolean or \"$b{ENV_VAR|default}\")"
    },
    "csvSource": {
      "anyOf": [
        {
          "title": "CSV source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "csv",
                  "description": "Source discriminator. Must be `csv`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `csv`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "delimiter": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1,
                  "description": "Single-character CSV field delimiter. Defaults to `,`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Single-character CSV field delimiter. Defaults to `,`. (string or \"$s{ENV_VAR|default}\")"
            },
            "geometryColumn": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "CSV column containing WKT geometry. Defaults to `geometry`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "CSV column containing WKT geometry. Defaults to `geometry`. (string or \"$s{ENV_VAR|default}\")"
            },
            "primaryKey": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "CSV column used as feature id. Defaults to `id`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "CSV column used as feature id. Defaults to `id`. (string or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "CSV file source with WKT geometry. Requires `type: \"csv\"` and `path`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "csv",
                  "description": "Source discriminator. Must be `csv`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `csv`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "delimiter": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 1,
                  "description": "Single-character CSV field delimiter. Defaults to `,`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Single-character CSV field delimiter. Defaults to `,`. (string or \"$s{ENV_VAR|default}\")"
            },
            "geometryColumn": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "CSV column containing WKT geometry. Defaults to `geometry`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "CSV column containing WKT geometry. Defaults to `geometry`. (string or \"$s{ENV_VAR|default}\")"
            },
            "primaryKey": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "CSV column used as feature id. Defaults to `id`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "CSV column used as feature id. Defaults to `id`. (string or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "CSV file source with WKT geometry. Requires `type: \"csv\"` and `path`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "CSV file source with WKT geometry. Requires `type: \"csv\"` and `path`. (object or \"#/$defs/nom\")"
    },
    "geojsonSource": {
      "anyOf": [
        {
          "title": "GeoJSON source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "geojson",
                  "description": "Source discriminator. Must be `geojson`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `geojson`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "GeoJSON file source. Requires `type: \"geojson\"` and `path`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "geojson",
                  "description": "Source discriminator. Must be `geojson`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `geojson`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "GeoJSON file source. Requires `type: \"geojson\"` and `path`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "GeoJSON file source. Requires `type: \"geojson\"` and `path`. (object or \"#/$defs/nom\")"
    },
    "gmlSource": {
      "anyOf": [
        {
          "title": "GML source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "gml",
                  "description": "Source discriminator. Must be `gml`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `gml`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "featureElementNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
            },
            "geometryPropertyNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
            },
            "axisOrder": {
              "anyOf": [
                {
                  "enum": [
                    "auto",
                    "xy",
                    "yx"
                  ],
                  "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`. (\"auto\", \"xy\", \"yx\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`. (\"auto\", \"xy\", \"yx\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "GML file source. Requires `type: \"gml\"` and `path`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "gml",
                  "description": "Source discriminator. Must be `gml`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `gml`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "featureElementNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "GML feature element names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
            },
            "geometryPropertyNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "GML geometry property names to read. Useful when auto-detection is ambiguous. (array or \"#/$defs/nom\")"
            },
            "axisOrder": {
              "anyOf": [
                {
                  "enum": [
                    "auto",
                    "xy",
                    "yx"
                  ],
                  "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`. (\"auto\", \"xy\", \"yx\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`. (\"auto\", \"xy\", \"yx\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "GML file source. Requires `type: \"gml\"` and `path`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "GML file source. Requires `type: \"gml\"` and `path`. (object or \"#/$defs/nom\")"
    },
    "shpSource": {
      "anyOf": [
        {
          "title": "Shapefile source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "shpPath",
            "dbfPath"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "shp",
                  "description": "Source discriminator. Must be `shp`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `shp`. (string or \"$s{ENV_VAR|default}\")"
            },
            "shpPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.shp` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Path to the `.shp` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
            },
            "dbfPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.dbf` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Path to the `.dbf` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
            },
            "dbfEncoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "Shapefile source. Requires `type: \"shp\"`, `shpPath` and `dbfPath`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "shp",
                  "description": "Source discriminator. Must be `shp`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `shp`. (string or \"$s{ENV_VAR|default}\")"
            },
            "shpPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.shp` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Path to the `.shp` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
            },
            "dbfPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.dbf` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Path to the `.dbf` file of the Shapefile source. (string or \"$s{ENV_VAR|default}\")"
            },
            "dbfEncoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`. (string or \"$s{ENV_VAR|default}\")"
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Stream buffer size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Shapefile source. Requires `type: \"shp\"`, `shpPath` and `dbfPath`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "Shapefile source. Requires `type: \"shp\"`, `shpPath` and `dbfPath`. (object or \"#/$defs/nom\")"
    },
    "gpkgSource": {
      "anyOf": [
        {
          "title": "GeoPackage source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "path",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "gpkg",
                  "description": "Source discriminator. Must be `gpkg`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `gpkg`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/gpkgDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "GeoPackage source. Requires `type: \"gpkg\"`, `path` and `datasets`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "gpkg",
                  "description": "Source discriminator. Must be `gpkg`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `gpkg`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/gpkgDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "GeoPackage source. Requires `type: \"gpkg\"`, `path` and `datasets`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "GeoPackage source. Requires `type: \"gpkg\"`, `path` and `datasets`. (object or \"#/$defs/nom\")"
    },
    "gpkgDatasets": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/gpkgDataset",
                "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "string",
                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
              },
              {
                "type": "string",
                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
              }
            ],
            "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
          },
          "description": "GeoPackage dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "GeoPackage dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/gpkgDataset",
                "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "string",
                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
              },
              {
                "type": "string",
                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
              }
            ],
            "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
          }
        }
      ],
      "description": "GeoPackage dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
    },
    "gpkgDataset": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "GeoPackage dataset mapping. Use a string for a simple table name, or an object for explicit table, geometry, primary key and property selection. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "GeoPackage dataset mapping. Use a string for a simple table name, or an object for explicit table, geometry, primary key and property selection. (string or \"$s{ENV_VAR|default}\")"
    },
    "sqlDatasets": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/sqlDataset",
                "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "string",
                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
              },
              {
                "type": "string",
                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
              }
            ],
            "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
          },
          "description": "SQL dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "SQL dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/sqlDataset",
                "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "string",
                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
              },
              {
                "type": "string",
                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
              }
            ],
            "description": "Value for each named entry in this registry. (string or \"$s{ENV_VAR|default}\")"
          }
        }
      ],
      "description": "SQL dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties. (object or \"#/$defs/nom\")"
    },
    "sqlDataset": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "srid": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Spatial reference identifier of the dataset geometry. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Spatial reference identifier of the dataset geometry. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                    },
                    "srid": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Spatial reference identifier of the dataset geometry. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Spatial reference identifier of the dataset geometry. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique. (array or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "SQL dataset mapping. Use a string for a simple table name, or an object for explicit schema, table, geometry, primary key, SRID and property selection. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "SQL dataset mapping. Use a string for a simple table name, or an object for explicit schema, table, geometry, primary key, SRID and property selection. (string or \"$s{ENV_VAR|default}\")"
    },
    "postgisSource": {
      "anyOf": [
        {
          "title": "PostGIS source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "connection",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "postgis",
                  "description": "Source discriminator. Must be `postgis`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `postgis`. (string or \"$s{ENV_VAR|default}\")"
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/postgisConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "estimated",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`. (\"estimated\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`. (\"estimated\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "PostGIS source. Requires `type: \"postgis\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "postgis",
                  "description": "Source discriminator. Must be `postgis`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `postgis`. (string or \"$s{ENV_VAR|default}\")"
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/postgisConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "estimated",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`. (\"estimated\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`. (\"estimated\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "PostGIS source. Requires `type: \"postgis\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "PostGIS source. Requires `type: \"postgis\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")"
    },
    "postgisConnection": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Short form using a string value. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Short form using a string value. (string or \"$s{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "host": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "port": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 65535,
                          "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "database": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "user": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "password": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "ssl": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether SSL/TLS is enabled for the database connection. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether SSL/TLS is enabled for the database connection. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "max": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum PostgreSQL pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum PostgreSQL pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "connectionTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "idleTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Idle connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Idle connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "statementTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "queryTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL query timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "PostgreSQL query timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "applicationName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Application name sent to PostgreSQL for connection identification. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Application name sent to PostgreSQL for connection identification. (string or \"$s{ENV_VAR|default}\")"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "host": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "port": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 65535,
                          "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "database": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "user": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "password": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "ssl": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether SSL/TLS is enabled for the database connection. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether SSL/TLS is enabled for the database connection. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "max": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum PostgreSQL pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum PostgreSQL pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "connectionTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "idleTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Idle connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Idle connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "statementTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "queryTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL query timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "PostgreSQL query timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "applicationName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Application name sent to PostgreSQL for connection identification. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Application name sent to PostgreSQL for connection identification. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "PostgreSQL/PostGIS connection configuration. Use either a connection string or an object with individual connection pool options. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "PostgreSQL/PostGIS connection configuration. Use either a connection string or an object with individual connection pool options. (string or \"$s{ENV_VAR|default}\")"
    },
    "mssqlSource": {
      "anyOf": [
        {
          "title": "SQL Server source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "connection",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "mssql",
                  "description": "Source discriminator. Must be `mssql`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `mssql`. (string or \"$s{ENV_VAR|default}\")"
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/mssqlConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. SQL Server allowed values: `exact`, `none`. Allowed values: `exact`, `none`. (\"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Strategy used to compute source extents. SQL Server allowed values: `exact`, `none`. Allowed values: `exact`, `none`. (\"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "SQL Server source. Requires `type: \"mssql\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "mssql",
                  "description": "Source discriminator. Must be `mssql`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `mssql`. (string or \"$s{ENV_VAR|default}\")"
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/mssqlConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. SQL Server allowed values: `exact`, `none`. Allowed values: `exact`, `none`. (\"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Strategy used to compute source extents. SQL Server allowed values: `exact`, `none`. Allowed values: `exact`, `none`. (\"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "SQL Server source. Requires `type: \"mssql\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "SQL Server source. Requires `type: \"mssql\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")"
    },
    "mssqlConnection": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Short form using a SQL Server connection string. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Short form using a SQL Server connection string. (string or \"$s{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "SQL Server connection string. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "SQL Server connection string. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "server": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "host": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Alias for SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Alias for SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "port": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 65535,
                          "description": "SQL Server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "SQL Server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "database": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "user": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "password": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "poolMin": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Minimum SQL Server pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Minimum SQL Server pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolMax": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum SQL Server pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum SQL Server pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "connectionTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "requestTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Request timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Request timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "encrypt": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether TLS encryption is enabled for the SQL Server connection. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether TLS encryption is enabled for the SQL Server connection. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "trustServerCertificate": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether the SQL Server certificate is trusted without validation. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether the SQL Server certificate is trusted without validation. (boolean or \"$b{ENV_VAR|default}\")"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "SQL Server connection string. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "SQL Server connection string. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "server": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "host": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Alias for SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Alias for SQL Server host name or IP address. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "port": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 65535,
                          "description": "SQL Server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "SQL Server port. Minimum: 1. Maximum: 65535. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "database": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "user": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database user name. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "password": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "poolMin": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Minimum SQL Server pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Minimum SQL Server pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolMax": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum SQL Server pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum SQL Server pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "connectionTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Connection timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "requestTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Request timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Request timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "encrypt": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether TLS encryption is enabled for the SQL Server connection. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether TLS encryption is enabled for the SQL Server connection. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "trustServerCertificate": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether the SQL Server certificate is trusted without validation. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether the SQL Server certificate is trusted without validation. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "SQL Server connection configuration. Use either a connection string or an object with individual connection pool options. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "SQL Server connection configuration. Use either a connection string or an object with individual connection pool options. (string or \"$s{ENV_VAR|default}\")"
    },
    "oracleSource": {
      "anyOf": [
        {
          "title": "Oracle source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "connection",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "oracle",
                  "description": "Source discriminator. Must be `oracle`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `oracle`. (string or \"$s{ENV_VAR|default}\")"
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "metadata",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`. (\"metadata\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`. (\"metadata\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            }
          },
          "unevaluatedProperties": false,
          "description": "Oracle Spatial source. Requires `type: \"oracle\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "oracle",
                  "description": "Source discriminator. Must be `oracle`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source discriminator. Must be `oracle`. (string or \"$s{ENV_VAR|default}\")"
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type. (string or \"$s{ENV_VAR|default}\")"
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers. (object or \"#/$defs/nom\")"
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "metadata",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`. (\"metadata\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`. (\"metadata\", \"exact\", \"none\" or \"$s{ENV_VAR|default}\")"
            },
            "indexes": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sourceIndexes"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ]
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Oracle Spatial source. Requires `type: \"oracle\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "Oracle Spatial source. Requires `type: \"oracle\"`, `connection` and `datasets`. (object or \"#/$defs/nom\")"
    },
    "oracleConnectionString": {
      "anyOf": [
        {
          "title": "Oracle connection URI",
          "type": "string",
          "pattern": "^oracle://[^:@/]+:[^@/]+@[^/]+:[0-9]+/.+$",
          "description": "Oracle connection URI. Expected format: `oracle://user:password@host:port/service`. Pattern: `oracle://user:password@host:port/service`, with a user, password, host, numeric port and service name. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "Oracle connection URI. Expected format: `oracle://user:password@host:port/service`. Pattern: `oracle://user:password@host:port/service`, with a user, password, host, numeric port and service name. (string or \"$s{ENV_VAR|default}\")"
    },
    "oracleConnection": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleConnectionString",
                  "description": "Short form: Oracle connection URI string matching `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Short form: Oracle connection URI string matching `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "connectionString"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/oracleConnectionString",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "externalAuth": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether Oracle external authentication is enabled. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether Oracle external authentication is enabled. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "homogeneous": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether the Oracle pool uses homogeneous authentication. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether the Oracle pool uses homogeneous authentication. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "poolMin": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Minimum Oracle pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Minimum Oracle pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolMax": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum Oracle pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum Oracle pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolIncrement": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of Oracle connections added when the pool grows. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of Oracle connections added when the pool grows. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle idle pool timeout in seconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle idle pool timeout in seconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "queueTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle queue timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle queue timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "stmtCacheSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle statement cache size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle statement cache size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "callTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle call timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle call timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "walletLocation": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle wallet directory. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path to the Oracle wallet directory. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "walletPassword": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "configDir": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle client/network configuration directory. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path to the Oracle client/network configuration directory. (string or \"$s{ENV_VAR|default}\")"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/oracleConnectionString",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "externalAuth": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether Oracle external authentication is enabled. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether Oracle external authentication is enabled. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "homogeneous": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether the Oracle pool uses homogeneous authentication. (boolean or \"$b{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Whether the Oracle pool uses homogeneous authentication. (boolean or \"$b{ENV_VAR|default}\")"
                    },
                    "poolMin": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Minimum Oracle pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Minimum Oracle pool size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolMax": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum Oracle pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Maximum Oracle pool size. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolIncrement": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of Oracle connections added when the pool grows. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Number of Oracle connections added when the pool grows. Minimum: 1. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "poolTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle idle pool timeout in seconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle idle pool timeout in seconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "queueTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle queue timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle queue timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "stmtCacheSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle statement cache size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle statement cache size. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "callTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle call timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Oracle call timeout in milliseconds. Minimum: 0. (integer or \"$i{ENV_VAR|default}\")"
                    },
                    "walletLocation": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle wallet directory. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path to the Oracle wallet directory. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "walletPassword": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "configDir": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle client/network configuration directory. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path to the Oracle client/network configuration directory. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "Oracle connection configuration. Use either an Oracle connection URI string or an object containing `connectionString` and optional pool/wallet settings. (string or \"$s{ENV_VAR|default}\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
        }
      ],
      "description": "Oracle connection configuration. Use either an Oracle connection URI string or an object containing `connectionString` and optional pool/wallet settings. (string or \"$s{ENV_VAR|default}\")"
    },
    "styles": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/style",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Style registry. Each property name is a style identifier. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Style registry. Each property name is a style identifier. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/style",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Style registry. Each property name is a style identifier. (object or \"#/$defs/nom\")"
    },
    "style": {
      "anyOf": [
        {
          "oneOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/builtinStyle",
                  "description": "First allowed object shape for this union. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "builtin",
                          "description": "Style discriminator. Must be `builtin`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Style discriminator. Must be `builtin`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "First allowed object shape for this union. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "First allowed object shape for this union. (object or \"#/$defs/nom\")"
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/dynamicStyle",
                  "description": "Second allowed object shape for this union. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "dynamic",
                          "description": "Style discriminator. Must be `dynamic`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Style discriminator. Must be `dynamic`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "options": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/dynamicStyleOptions",
                          "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "units": {
                              "anyOf": [
                                {
                                  "enum": [
                                    "m",
                                    "dd"
                                  ],
                                  "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                }
                              ],
                              "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                            },
                            "dotsPerInch": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "exclusiveMinimum": 0,
                                  "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                }
                              ],
                              "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                            },
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            }
                          },
                          "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                        }
                      ],
                      "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Second allowed object shape for this union. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Second allowed object shape for this union. (object or \"#/$defs/nom\")"
            }
          ],
          "description": "Style definition. Choose either a built-in style or a dynamic style file."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        }
      ],
      "description": "Style definition. Choose either a built-in style or a dynamic style file."
    },
    "builtinStyle": {
      "anyOf": [
        {
          "title": "Builtin style",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "builtin",
                  "description": "Style discriminator. Must be `builtin`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style discriminator. Must be `builtin`. (string or \"$s{ENV_VAR|default}\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "Built-in renderer style. Requires `type: \"builtin\"`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "builtin",
                  "description": "Style discriminator. Must be `builtin`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style discriminator. Must be `builtin`. (string or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Built-in renderer style. Requires `type: \"builtin\"`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "Built-in renderer style. Requires `type: \"builtin\"`. (object or \"#/$defs/nom\")"
    },
    "dynamicStyle": {
      "anyOf": [
        {
          "title": "Dynamic style file",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "dynamic",
                  "description": "Style discriminator. Must be `dynamic`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style discriminator. Must be `dynamic`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "options": {
              "anyOf": [
                {
                  "$ref": "#/$defs/dynamicStyleOptions",
                  "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "units": {
                      "anyOf": [
                        {
                          "enum": [
                            "m",
                            "dd"
                          ],
                          "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                    },
                    "dotsPerInch": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "Dynamic style loaded from a file. Requires `type: \"dynamic\"` and `path`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "type": {
              "anyOf": [
                {
                  "const": "dynamic",
                  "description": "Style discriminator. Must be `dynamic`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style discriminator. Must be `dynamic`. (string or \"$s{ENV_VAR|default}\")"
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`. (string or \"$s{ENV_VAR|default}\")"
            },
            "options": {
              "anyOf": [
                {
                  "$ref": "#/$defs/dynamicStyleOptions",
                  "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "units": {
                      "anyOf": [
                        {
                          "enum": [
                            "m",
                            "dd"
                          ],
                          "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                    },
                    "dotsPerInch": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        }
                      ],
                      "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Options passed to this style or renderer. (object or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Dynamic style loaded from a file. Requires `type: \"dynamic\"` and `path`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "Dynamic style loaded from a file. Requires `type: \"dynamic\"` and `path`. (object or \"#/$defs/nom\")"
    },
    "dynamicStyleOptions": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "units": {
              "anyOf": [
                {
                  "enum": [
                    "m",
                    "dd"
                  ],
                  "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
            },
            "dotsPerInch": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
            }
          },
          "description": "Rendering options passed to a dynamic style. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "units": {
              "anyOf": [
                {
                  "enum": [
                    "m",
                    "dd"
                  ],
                  "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`. (\"m\", \"dd\" or \"$s{ENV_VAR|default}\")"
            },
            "dotsPerInch": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                }
              ],
              "description": "Rendering DPI used by the dynamic style. Must be greater than 0. (number or \"$f{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Rendering options passed to a dynamic style. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Rendering options passed to a dynamic style. (object or \"#/$defs/nom\")"
    },
    "layers": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/layer",
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "source": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "layer": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "dataset": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1,
                        "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                  },
                  "crs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "extent": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/bbox",
                        "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
                  },
                  "style": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "pointProperties": {
                    "anyOf": [
                      {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "$ref": "#/$defs/pointPropertyPair",
                              "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "object",
                              "required": [
                                "$ref"
                              ],
                              "properties": {
                                "x": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "y": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "crs": {
                                  "anyOf": [
                                    {
                                      "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "$ref": {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                }
                              },
                              "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                            }
                          ],
                          "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                        },
                        "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
          },
          "description": "Layer registry. At least one published layer is required. Each property name is the public layer identifier. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Layer registry. At least one published layer is required. Each property name is the public layer identifier. (object or \"#/$defs/nom\")",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/layer",
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "source": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "layer": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "dataset": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1,
                        "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                  },
                  "crs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "extent": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/bbox",
                        "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
                  },
                  "style": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                      }
                    ],
                    "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                  },
                  "pointProperties": {
                    "anyOf": [
                      {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "$ref": "#/$defs/pointPropertyPair",
                              "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                            },
                            {
                              "type": "object",
                              "required": [
                                "$ref"
                              ],
                              "properties": {
                                "x": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "y": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "crs": {
                                  "anyOf": [
                                    {
                                      "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                                    }
                                  ],
                                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                                },
                                "$ref": {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                                }
                              },
                              "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                            }
                          ],
                          "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                        },
                        "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                  }
                },
                "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
              }
            ],
            "description": "Value for each named entry in this registry. (object or \"#/$defs/nom\")"
          }
        }
      ],
      "description": "Layer registry. At least one published layer is required. Each property name is the public layer identifier. (object or \"#/$defs/nom\")"
    },
    "layer": {
      "anyOf": [
        {
          "title": "Layer",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                    }
                  },
                  "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`. (object or \"#/$defs/nom\")"
            }
          ],
          "oneOf": [
            {
              "anyOf": [
                {
                  "required": [
                    "source",
                    "crs"
                  ],
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "layer"
                        ]
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ]
                  },
                  "description": "First allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "First allowed object shape for this union."
            },
            {
              "anyOf": [
                {
                  "required": [
                    "layer"
                  ],
                  "not": {
                    "anyOf": [
                      {
                        "anyOf": [
                          {
                            "anyOf": [
                              {
                                "required": [
                                  "source"
                                ]
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              }
                            ]
                          },
                          {
                            "anyOf": [
                              {
                                "required": [
                                  "dataset"
                                ]
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              }
                            ]
                          }
                        ],
                        "description": "Alias layers must not define `source` or `dataset`; they point to another layer through `layer`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      }
                    ],
                    "description": "Alias layers must not define `source` or `dataset`; they point to another layer through `layer`."
                  },
                  "description": "Second allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Second allowed object shape for this union."
            }
          ],
          "properties": {
            "source": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
            },
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
            },
            "dataset": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "crs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
            },
            "extent": {
              "anyOf": [
                {
                  "$ref": "#/$defs/bbox",
                  "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
            },
            "pointProperties": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/pointPropertyPair",
                        "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "x": {
                            "anyOf": [
                              {
                                "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "y": {
                            "anyOf": [
                              {
                                "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "crs": {
                            "anyOf": [
                              {
                                "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                          }
                        },
                        "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                  },
                  "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
            }
          },
          "unevaluatedProperties": false,
          "description": "Published layer definition. Either define a source-backed layer with `source` and `crs`, or alias another layer with `layer`. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients. (string or \"$s{ENV_VAR|default}\")"
            },
            "source": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Source name. Must reference a key from the top-level `sources` registry. (string or \"$s{ENV_VAR|default}\")"
            },
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer. (string or \"$s{ENV_VAR|default}\")"
            },
            "dataset": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s). (string or \"$s{ENV_VAR|default}\")"
            },
            "crs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
            },
            "extent": {
              "anyOf": [
                {
                  "$ref": "#/$defs/bbox",
                  "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required. (array or \"#/$defs/nom\")"
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry. (string or \"$s{ENV_VAR|default}\")"
            },
            "pointProperties": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/pointPropertyPair",
                        "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "x": {
                            "anyOf": [
                              {
                                "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "y": {
                            "anyOf": [
                              {
                                "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "crs": {
                            "anyOf": [
                              {
                                "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                              }
                            ],
                            "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                          }
                        },
                        "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates. (object or \"#/$defs/nom\")"
                  },
                  "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                }
              ],
              "description": "List of X/Y property pairs to reproject as point coordinates. (array or \"#/$defs/nom\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Published layer definition. Either define a source-backed layer with `source` and `crs`, or alias another layer with `layer`. (object or \"#/$defs/nom\")",
          "unevaluatedProperties": false
        }
      ],
      "description": "Published layer definition. Either define a source-backed layer with `source` and `crs`, or alias another layer with `layer`. (object or \"#/$defs/nom\")"
    },
    "pointPropertyPair": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "x",
            "y"
          ],
          "properties": {
            "x": {
              "anyOf": [
                {
                  "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
            },
            "y": {
              "anyOf": [
                {
                  "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
            },
            "crs": {
              "anyOf": [
                {
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
            }
          },
          "description": "Pair of feature property names containing X/Y coordinates that should be reprojected along with the layer. (object or \"#/$defs/nom\")"
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "x": {
              "anyOf": [
                {
                  "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Property name containing the X coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
            },
            "y": {
              "anyOf": [
                {
                  "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Property name containing the Y coordinate to reproject. (string or \"$s{ENV_VAR|default}\")"
            },
            "crs": {
              "anyOf": [
                {
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. (string or \"$s{ENV_VAR|default}\")"
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$"
            }
          },
          "description": "Pair of feature property names containing X/Y coordinates that should be reprojected along with the layer. (object or \"#/$defs/nom\")",
          "additionalProperties": false
        }
      ],
      "description": "Pair of feature property names containing X/Y coordinates that should be reprojected along with the layer. (object or \"#/$defs/nom\")"
    }
  }
}
